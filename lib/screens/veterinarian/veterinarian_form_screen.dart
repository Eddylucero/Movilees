import 'package:flutter/material.dart';

class VeterinarianFormScreen extends StatelessWidget {
  const VeterinarianFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
